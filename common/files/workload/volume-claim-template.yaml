metadata:
  name: {{ .name }}
spec:
  accessModes:
  - ReadWriteOnce
  {{- with .storageClassName }}
  storageClassName: {{ . }}
  {{- end }}
  resources:
    requests:
      storage: {{ .size | default "1Gi" }}